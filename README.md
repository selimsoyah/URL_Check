# URL Check

Validates a URL as you type and checks (via a mocked server) whether it exists and if it is a file or folder.

## Run

Requires Node.js and npm.

```bash
npm install
npm run dev
```

Open http://localhost:5173 in your browser.

## Build

```bash
npm run build
npm run preview
```
